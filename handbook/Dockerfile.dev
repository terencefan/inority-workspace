FROM node:22-bookworm-slim

RUN apt-get update \
    && apt-get install -y --no-install-recommends \
        fontconfig \
        fonts-noto-cjk \
        graphviz \
    && rm -rf /var/lib/apt/lists/*

WORKDIR /workspace/inority-workspace/handbook

CMD ["npm", "run", "dev"]
