interface:
  display_name: "Checkout / Checkin"
  short_description: "Publish at checkout; sync only on explicit checkin."
  default_prompt: "Use $checkout for an end-of-day publish sweep, or explicitly say checkin / 上班 to run its sync-only workspace mode."
