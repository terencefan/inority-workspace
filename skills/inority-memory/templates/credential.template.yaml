# Local-only credential file.
# Fill in real values only on this machine or workspace copy.
# Do not copy populated credentials back into `inority-workspace`.

# Example:
# registry:
#   username: your-username
#   password: your-password
