D010:
  message: "Markdown 内嵌 DOT 图必须显式设置透明背景 `bgcolor=\"transparent\"`"
D011:
  message: "DOT 图的 node 默认属性必须显式包含 filled 样式"
D012:
  message: "box 类 node 默认属性必须显式包含 rounded 样式"
D013:
  message: "DOT 图的 node 默认属性必须显式设置边框颜色 `color`"
D014:
  message: "DOT 图的 node 默认属性必须显式设置文字颜色 `fontcolor`"
D015:
  message: "DOT 图的 node 默认属性必须显式设置填充色 `fillcolor`"
D020:
  message: "DOT 图的 edge 默认属性必须显式设置线条颜色 `color`"
D021:
  message: "DOT 图的 edge 默认属性必须显式设置文字颜色 `fontcolor`"
D022:
  message: "包含中日韩文字的可移植 DOT 图必须在 graph、node 和 edge 上统一设置 `fontname=\"sans-serif\"`，禁止默认 Times 或仅生成端可用的字体"
D030:
  message: "透明背景下的 cluster 必须显式设置边框颜色 `color`"
D031:
  message: "透明背景下的 cluster 必须显式设置标题颜色 `fontcolor`"
D040:
  message: "Markdown 文档里没有找到 `dot` 或 `graphviz` 代码块"
D090:
  message: "Graphviz render smoke check 失败"
D091:
  message: "本地未检测到 Graphviz `dot` 命令；必须安装 Graphviz 后重新执行 render smoke check"
