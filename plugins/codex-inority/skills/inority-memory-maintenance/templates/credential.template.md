# Credential Index

This local-only file is the credential entrypoint. Keep secret values in
categorized `credential.d/*.yaml` fragments and list each fragment here.

## Lookup

No credential fragments have been configured yet.

## Rules

- Read only the fragment required by the current task.
- Never copy populated credential fragments back into `inority-workspace`.
- Update this index whenever adding or moving a credential fragment.
