interface:
  display_name: "Inority Reply"
  short_description: "管理 inority reply hook、模板与运行时安装。"
  default_prompt: "Use $inority-reply to inspect or refine the codex-inority host-aware reply contract and its plugin hook behavior."
policy:
  allow_implicit_invocation: false
