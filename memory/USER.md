# USER.md

记录 Terence 的个人偏好。这里不保存团队通用偏好或具体 skill 规则。

## 用户画像

- 姓名：Terence Fan
- 称呼：Terence
- 时区：UTC+08:00

## 沟通与决策

- 除非 Terence 明确要求其他语言，否则默认用中文回复，并用中文编写工作区文档。
- 呈现列表时，优先清晰的层次和有组织的结构，不一味平铺。
- Terence 纠正工作流或输出契约后，更新对应受管源文件，不只修正当前回复。
- 在 tmux 或终端里提供长链接时，优先使用 `OSC 8` 超链接或短文本包装；终端不支持时，再提供完整可复制 URL 或将其落盘到 `tmp/`。

## 权限与安全

- 任何离开本机、代表 Terence 对外发言或具有破坏性的操作都先征求确认；存在可恢复方案时，不选择不可逆手段。
- 使用飞书等外部系统发送消息时，默认使用 bot 身份。只有 Terence 明确要求个人身份时，才使用 user 身份发送。
- `MogTracker/` 下的常规编辑已默认预授权，无需事前询问。
- `.codex/memory/dairy/*.md` 可以直接做非破坏性编辑；删除、归档或移动日记文件前仍需询问。
- 删除工作区文件时，优先移到 `/home/fantengyuan/workspace/.recycle/` 并保留相对路径，不永久删除。

## 文档写作

- `aiready` 是固定术语：普通小写写作 `aiready`，大写、标题或专名写作 `AIReady`；不写成 `ai-ready`、`AI-Ready` 或 `AIREADY`。
- blockquote 保持简洁，优先只写一句；必须写多句时分行表达。
- 更新飞书文档时，如果漂移检查仅发现 revision 变化、线上正文没有变化，默认直接刷新基线后发布，不再询问合并路径。若线上正文确有变化，仍先生成差异并确认合并方式，避免覆盖人工编辑。

## 环境偏好

- 后续出网请求和镜像构建优先使用实验室 HTTP 代理，Python 依赖优先使用实验室 PyPI；实验室链路不可用并明确说明后，才考虑 MonoCloud 或公共 PyPI。
- 默认在 WSL 中启动本地服务，不直接在 Windows 宿主启动；需要提权或系统级安装时，优先使用 `wsl -u root`，不使用当前用户下的交互式 `sudo`。
